npm run build # may not be needed as it is running dev
odo  create nodejs
#   odo create nodejs --port 3000/tcp  # not tested
odo push # first push takes quite long, ~5min
# need to update port to 3000 on pod, service, route
